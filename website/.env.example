# Example environment variables for local dev and CI.
# Copy to `.env.local` (ignored by git) and fill in the values you need.

# Full GoatCounter count endpoint, e.g. https://<code>.goatcounter.com/count.
# Leave unset to disable analytics entirely (the useAnalytics hook no-ops).
VITE_GOATCOUNTER_ENDPOINT=
